# Remaining Issues

- The waveform is CSS-made and less organic than the generated image.
- The screen is static; a production app would need real routing, keyboard focus, and responsive
  checks.
- The product itself is fictional, so copy and data are illustrative.
- The final UI is intentionally simple. A real product would need the session screen and review flow
  designed next.
