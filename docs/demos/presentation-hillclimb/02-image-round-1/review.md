# Image Round 1 Review

Stage: generated-variant review.
Consumed: `02-image-round-1/variants/variant-a.png`, `variant-b.png`, `variant-c.png`.

| Variant | Focus | Clutter | Information value | Verdict |
| --- | --- | --- | --- | --- |
| A | Strong task focus; CTA is obvious | Low | Skill snapshot is secondary and readable | Selected base |
| B | Strong task card, but right panel competes | Medium | Reintroduces coach preview and privacy boilerplate | Reject as direction |
| C | Memorable central task in a path | Medium | Path adds future steps and locks that distract | Use one idea only |

## Findings

- High / B / coach-prep panel repeats the original problem / it previews feedback before the user
  records / reject the right-panel strategy.
- High / C / path UI makes the task visually clear but adds future steps and locks / harvest the
  central unlocked-task idea, not the full path.
- Medium / A / typography is cleaner than the source, but the hero card is slightly too ceremonial /
  keep the centered hierarchy but make it feel more like a product task than a poster.
- Medium / A / `View full progress` adds a secondary route / remove from next round unless progress
  is the user's next needed action.

## Selected Direction

Use Variant A as the base.

Harvest from Variant C:

- the sense that the current task is unlocked and central
- the compact skill snapshot below the main task

Reject:

- B's coaching-prep side panel
- C's visible future/locked steps
- generated extra links
